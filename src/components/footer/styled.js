import styled from 'styled-components';

export const FooterComponent = styled.header `
    width: 100%;
    height: 40px;
    background-color: rgba(99, 204, 167, 0.6);
    display: flex;
    justify-content: center;
    align-items: center;
`

export const CopyrightText = styled.p `
    margin: 0;
    padding: 0;
    font-size: 12px;
`